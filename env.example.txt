# Required: obtain this from AbuseIPDB.
ABUSEIPDB_API_KEY=

# Optional: leave empty to disable VirusTotal enrichment.
VIRUSTOTAL_API_KEY=

# Network listener
HOST=0.0.0.0
PORT=3443

# TLS files. Relative paths are resolved from the application folder.
CERT_DIR=certs
# TLS_KEY_FILE=/run/secrets/server.key
# TLS_CERT_FILE=/run/secrets/server.crt

# Temporary workaround only for a TLS-inspecting corporate proxy.
# Prefer CORPORATE_CA_FILE instead; this disables upstream certificate validation.
# CORPORATE_PROXY_INSECURE_TLS=false
